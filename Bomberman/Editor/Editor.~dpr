program Editor;

uses
  glApplication,
  edPanels in 'edPanels.pas',
  edMain in 'edMain.pas';

{$R Resource.res}

begin
  Application.Initialize;
  Application.CreateForm(TWindow1, Window1);
  Application.CreateGraphics(TGraphics1, Graphics1);
  Application.Run;
end.
